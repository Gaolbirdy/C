#include <stdio.h>
#include <conio.h>
#include <locale.h>
#include <time.h>

//void main()
//{
//	time_t curtime;
//	struct tm *timer;
//	char buffer[80];
//
//	time(&curtime);
//	timer = localtime(&curtime);
//
//	printf("Locale is : %s\n", setlocale(LC_ALL, "en_GB"));
//	strftime(buffer, 80, "%c", timer);
//	printf("Date is : %s\n", buffer);
//
//	printf("Locale is : %s\n", setlocale(LC_ALL, "de_DE"));
//	strftime(buffer, 80, "%c", timer);
//	printf("Date is : %s\n", buffer);
//
//	getch();
//}

void main()
{

	struct lconv
	getch();
}